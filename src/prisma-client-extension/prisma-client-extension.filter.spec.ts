import { PrismaClientExtensionFilter } from './prisma-client-extension.filter';

describe('PrismaClientExtensionFilter', () => {
  it('should be defined', () => {
    expect(new PrismaClientExtensionFilter()).toBeDefined();
  });
});
